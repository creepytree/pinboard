"""pinboard - super simple web-dashboard for url collections and notes.

Deliberately empty: the application lives in ``pinboard.app`` so that importing
the package (e.g. via the ``pinboard`` console script) does not freeze the
env-derived configuration before the CLI has exported its variables.
"""
